# Build de produção: chamadas de API relativas à própria origem (o
# backend serve o frontend na mesma porta via ServeStaticModule), em vez
# de embutir http://localhost:3000 fixo no bundle.
VITE_API_URL=
